The data directory
==================

The data directory is a directory over which the pipeline has complete control. Raw data is exported to it,
processed data is saved there and the models look there for their training and testing data.
